pipeline {
    agent any

    stages {

        stage('Checkout Verification') {
            steps {
                echo "Repository cloned successfully"

                sh 'pwd'
                sh 'ls -la'
            }
        }

    }
}